# PeerJS Signaling Server Configuration
# Заполни VITE_PEER_HOST чтобы переключиться на домашний сервер.
# Если оставить пустым — используется публичный 0.peerjs.com (fallback).

VITE_PEER_HOST=
VITE_PEER_PORT=9000
VITE_PEER_SECURE=false
